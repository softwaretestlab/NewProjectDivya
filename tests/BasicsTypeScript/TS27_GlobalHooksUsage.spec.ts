import { test, expect } from '../../fixtures/fixtures';

test.describe('Admin Tests', () => {
test('Sample test', async ({ page }) => {
  await page.goto('https://www.google.com/');
 
});

test('david test2', async ({ page }) => {
  await page.goto('https://www.davidjones.com/');
 
});
});